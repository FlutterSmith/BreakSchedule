name: break_reminder_app
description: A Break Reminder App using BloC and Clean Architecture.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.2
  equatable: ^2.0.5
  flutter_local_notifications: ^13.0.0
  lottie: ^2.2.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  lint: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/lottie/setup_header.json
